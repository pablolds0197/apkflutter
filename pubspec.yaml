name: roless
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutterflu
  
  flutter_launcher_icons: "^0.7.0"


flutter:
  uses-material-design: true
  assets:
    - assets/logo-twbs-negro.png

flutter_launcher_icons:
  ios: true
  android: true
  image_path: "assets/logo-twbs-negro.png"